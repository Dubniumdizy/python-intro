# HW FEEDBACK

HW1 feedback
1. Make the several prints to a docstring
2. Add the comments from above
3. Make a while loop that is always true looking for user input
4. Space between operators but not functions
5. Add a main() and if __name__ == "__main__": for calling the many functions
6. Use two empty rows between global functions, classes, import to first function/class. 
Use one empty row between smaller logic like methods in a class, inside a function body

HW2 feedback
1. Make hexa_chars a global variable
2. Add the comments from above
3. Use find()/index() in 1b. Python strings only allow you to look up items using integers
4. Hexa_to_deci lacks a return statment
5. Use two empty rows between global functions, classes, import to first function/class. 
Use one empty row between smaller logic like methods in a class, inside a function body

HW3 feedback
1. Spacing with operators, after commas
2. You should never explicitly compare a boolean to True or False. Instead, write it 
natively using the not keyword: if not inserted:
3. close() became cloe()
4. In your insert_in_sorted function, you create a brand new list (new_list), copy elements over, 
and return it. This works perfectly! However, keep in mind that the assignment instructions ask 
to insert an element into an already sorted list. If your teacher expects you to modify the 
list directly in place rather than creating a new one, try using the native .insert() method.
