{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build LaTeX PDF (current file)",
      "type": "shell",
      "command": "$pdf='${fileBasenameNoExtension}.pdf'; pdflatex -interaction=nonstopmode -synctex=1 -file-line-error -halt-on-error ${fileBasename}; if (Test-Path $pdf) { Write-Host \"PDF generated: $pdf\"; exit 0 } else { exit 1 }",
      "args": [
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
